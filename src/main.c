# include "../include/memory.h"

int main(void)
{
    char *str = malloc(50);
    char *str2 = malloc(50);
    // str = "Hello World";
    // printf("str string = %s\n", str);
    // printf("str *p = %p\n", str);
    return 0;
}